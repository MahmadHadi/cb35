// 100 98 96 94...2
#include <stdio.h>
void main()
{
      int count = 99;

      while (count > 0)
      {
            printf("%d ", count);
            count -= 2;
      }

      // printf("%d",count);

      // count-=2;
      // printf("%d",count);

      // count-=2;
      // printf("%d",count);
}