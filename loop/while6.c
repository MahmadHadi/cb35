// write a program that print 100 to 1
#include <stdio.h>
void main()
{
      int count = 100;

      while (count >= 1)
      {
            /* code */
            printf("%d ", count);
            count--;
      }

      // printf("%d", count);
      // count--;
      // printf("%d", count);

      // count--;
      // printf("%d", count);
      // count--;
      // printf("%d", count);
      // count--;
      // printf("%d", count);
}